"""
json_templates.py
=================
Шаблоны структуры JSON и функции создания базового состояния
для системы синхронизации коллекций с несколькими tracked-папками.

Структура оптимизирована под:
- быстрый доступ к файлам (index)
- уникальные alias (Проект+Папка)
- хранение дерева children для каждого tracked
- лёгкое добавление новых полей в будущем
"""

import datetime
from typing import Dict, Any
from copy import deepcopy


# ──────────────────────────────────────────────
# 🧩 ШАБЛОН КОЛЛЕКЦИИ
# ──────────────────────────────────────────────
COLLECTION_TEMPLATE: Dict[str, Any] = {
    "path": "",               # путь к локальной коллекции
    "last_scan": "",          # время последнего сканирования
    "children": [],           # структура файлов и папок внутри коллекции
    "hash": ""                # общий хеш коллекции
}


# ──────────────────────────────────────────────
# 🧩 ШАБЛОН TRACKED (отслеживаемая папка)
# ──────────────────────────────────────────────
TRACKED_TEMPLATE: Dict[str, Any] = {
    "alias": "",              # уникальное имя "Проект+Папка"
    "project_name": "",       # имя проекта (из пути)
    "raw_alias": "",          # имя самой папки (последняя директория)
    "path": "",               # абсолютный путь к папке на сервере
    "enabled": True,          # возможность временно отключить синхронизацию
    "hash": "",               # хеш всей папки
    "last_scan": "",          # время последнего сканирования
    "children": []            # структура вложенных файлов / директорий
}


# ──────────────────────────────────────────────
# ⚙️ ОСНОВНАЯ ФУНКЦИЯ СОЗДАНИЯ JSON
# ──────────────────────────────────────────────
def create_initial_data() -> Dict[str, Any]:
    """
    Создаёт минимальную структуру JSON, полностью пригодную
    для работы системы синхронизации.

    Используется при первом запуске, если JSON ещё не создан.
    """

    return {
        # Метаданные файла состояния
        "meta": {
            "version": 1,
            "created": datetime.datetime.now().isoformat(),
            "last_global_scan": ""
        },

        # Основная локальная коллекция (куда всё копируется)
        "collection": deepcopy(COLLECTION_TEMPLATE),

        # Список отслеживаемых папок (каждая — клон TRACKED_TEMPLATE)
        "tracked": [],

        # Индекс файлов для быстрого доступа
        # ключ: "Проект+Папка:/relative/path/to/file.ext"
        "index": {},

        # Конфликты синхронизации
        "conflicts": {
            "unresolved": [],
            "resolved": [],
            "ignored": []
        },

        # Системная информация
        "system": {
            "total_files": 0,            # сколько файлов известно
            "last_update": "",           # когда обновлялся index
            "scan_tasks": 0,             # количество выполненных задач сканирования
        }
    }
